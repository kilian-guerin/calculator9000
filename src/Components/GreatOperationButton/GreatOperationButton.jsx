import './GreatOperationButton.css';

function GreatOperationButton() {
    // document.addEventListener('click', function(e) {
    //     e = e || window.event;
    //     var target = e.target
    //     if(target.textContent === "=" || target.textContent === "+" || target.textContent === "-" || target.textContent === "*" || target.textContent === "/") {
    //         return target.e.textContent;
    //     }
    // }, false)

    return (
        <button>=</button>
    );
}

export default GreatOperationButton;